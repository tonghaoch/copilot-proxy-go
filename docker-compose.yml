version: "3.8"

services:
  copilot-proxy:
    build: .
    ports:
      - "4141:4141"
    environment:
      - GH_TOKEN=${GH_TOKEN}
    volumes:
      - ./copilot-data:/root/.local/share/copilot-api
    restart: unless-stopped
